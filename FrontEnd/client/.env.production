# `vite build` / deploy — API REST (có dấu / ở cuối cũng được, app sẽ chuẩn hoá).
# Nếu trên Vercel/… bạn từng set VITE_API_URL=http://localhost:5000 → xoá hoặc đổi; code cũng tự
# bỏ qua localhost khi chạy trên domain thật (nhongplus.id.vn).
VITE_API_URL=https://api.nhongplus.id.vn
