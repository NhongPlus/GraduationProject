# Chỉ dùng khi `npm run dev` — không ảnh hưởng `vite build`.
VITE_API_URL=http://localhost:5000
# Bỏ comment dòng dưới nếu muốn test làm bài không bắt fullscreen (chỉ local):
# VITE_DISABLE_INTEGRITY=true
