PORT=5000
APP_PORT=5000
# Neon database connection
DATABASE_URL=your_database_url
# JWT secret key
JWT_SECRET=your_secret_key
# token expire time
JWT_EXPIRES_IN=7d
# CORS + Socket.IO: danh sách origin, cách nhau bởi dấu phẩy (mặc định trong code: localhost:5173 + https://nhongplus.id.vn)
# CORS_ORIGINS=http://localhost:5173,https://nhongplus.id.vn,https://www.nhongplus.id.vn

# --- SMTP (nhắc hạn thi 24h/1h, reset password qua mail) ---
# Bắt buộc tối thiểu: SMTP_HOST + MAIL_FROM (xem mail.service.ts)
# Hầu hết máy chủ SMTP cần thêm SMTP_USER + SMTP_PASS
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=
# SMTP_PASS=
# MAIL_FROM=noreply@example.com

# Chu kỳ quét nhắc hạn (ms), mặc định 600000 = 10 phút
# EXAM_REMINDER_INTERVAL_MS=600000